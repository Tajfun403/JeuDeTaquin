#include "Graph.h"

char* GenerateAveragesDB(PlotItem** arr)
{
	// generate a gnuplot DB holding average values
	// return a file name
	return nullptr;
}

char* GeneratePointsDB(PlotItem** arr)
{
	// generate a gnuplot DB holding singular points
	// return a file name
	return nullptr;
}

void GenerateGraph(PlotItem** arr)
{
	// get both data sets
	// feed both to gnuplot. One with lines, the other without
}
